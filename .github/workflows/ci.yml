name: CI

on:
  pull_request:
    branches: [ main ]
  push:
    tags:
      - "v*"
  workflow_dispatch: # allows manual run too

jobs:
  build:
    name: CI
    runs-on: ubuntu-latest
    env:
      BUILD_IMAGE: fozztexx/defoogi:1.4.5

    outputs:
      files: ${{ steps.list_zips.outputs.files }}

    steps:
      - uses: actions/checkout@v4

      - name: Pull build image (with retries)
        shell: bash
        run: |
          for attempt in {1..10}; do
            if docker pull "$BUILD_IMAGE"; then
              exit 0
            fi
            echo "docker pull failed (attempt $attempt), retrying..."
            sleep 15
          done
          echo "Failed to pull $BUILD_IMAGE after 10 attempts"
          exit 1

      - name: Set version string
        shell: bash
        run: |
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            version="${{ github.ref_name }}"
            echo "VERSION_STRING=${version#v}" >> "$GITHUB_ENV"
          else
            echo "VERSION_STRING=${GITHUB_SHA:0:7}" >> "$GITHUB_ENV"
          fi

      - name: Build release files
        shell: bash
        run: |
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -e VERSION_STRING \
            -v "$PWD:/work" \
            -w /work \
            "$BUILD_IMAGE" \
            make release

      - name: List zip files
        id: list_zips
        shell: bash
        run: |
          echo 'files=["'"$(( cd dist ; echo *.zip ) | sed -e 's/ /","/g')"'"]' >> $GITHUB_OUTPUT

      - name: Upload dist folder for next job
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  upload:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    strategy:
      matrix:
        file: ${{ fromJson(needs.build.outputs.files) }}
    steps:
      - uses: actions/checkout@v4

      - name: Download dist folder
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Upload individual artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.file }}
          path: dist/${{ matrix.file }}

  tagged-release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v4

      # Download the dist folder from the build job
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      # Generate random release name
      - name: Generate random release name
        id: random_name
        run: |
          RANDOM_NAME=$(docker run --rm fnichol/names)
          echo "name=$RANDOM_NAME" >> $GITHUB_OUTPUT
          echo "Generated release name: $RANDOM_NAME"

      # Create GitHub Release for the tag
      - name: Create GitHub Release
        id: create_release
        uses: actions/create-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          release_name: ${{ steps.random_name.outputs.name }} (${{ github.ref_name }})
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Upload each zip as a release asset
      - name: Upload release assets
        run: |
          for zip in dist/*.zip; do
            echo "Uploading $zip..."
            gh release upload ${{ github.ref_name }} "$zip"
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
